/**
 * Panel build version shown in the top bar so it's always obvious which build
 * Premiere actually loaded (CEP caches panels aggressively). Bump alongside
 * package.json and CSXS/manifest.xml.
 */
export const PANEL_VERSION = "0.2.1";
